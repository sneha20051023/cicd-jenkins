pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Example: compile code
                // sh 'javac MyApp.java'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing...'
                // Example: run tests
                // sh 'java org.junit.runner.JUnitCore MyAppTest'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Example: deploy script
                // sh './deploy.sh'
            }
        }
    }
}
